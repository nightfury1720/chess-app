export const validMoveChecker = (inititalPosition: string, finalPosition: string) => {
    return true;
}

export const movePlayed = (inititalPosition: string, finalPosition: string) => {
    return;
}